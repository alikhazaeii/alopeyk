import { Harmattan , Gulzar } from 'next/font/google';

export  const harmattan = Harmattan({
  subsets: ['arabic'], 
  weight: ['400', '700'], 
  
});

export const gulzar = Gulzar({
  subsets : ['arabic'],
  weight:['400']
})